// Example content of the inventory.ts file (this needs to be replaced with the actual file content) 

// Assuming storage is defined somewhere in the file
const storage: Record<string, any[]> = {};  

const equipmentCategories = ['weapons', 'armor', 'potions'];

// Example usage with the updated type
const getEquipment = (category: string) => {
    return storage[category];  
};

// Full content of the file will be provided here after fetching the existing content.